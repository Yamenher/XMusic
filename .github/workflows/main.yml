name: Android CI

on:
  push:
    branches: [ "**" ]
    paths-ignore:
      - '**/*.md'
  pull_request:
    branches: [ "**" ]
    paths-ignore:
      - '**/*.md'
  workflow_dispatch:

jobs:
  init:
    name: Init
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: notify telegram
        uses: EverythingSuckz/github-telegram-notify@main
        continue-on-error: true
        with:
          bot_token: ${{ secrets.bot_token }}
          chat_id: -1002951791106
          topic_id: 4
          
  build_debug_apk:
    name: Build App Debug APKs
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'oracle'
          cache: gradle

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Validate Gradle wrapper
        uses: gradle/actions/wrapper-validation@v4

      - name: Build both ABIs
        run: ./gradlew assembleDebug -PTG_BOT_TOKEN="${{ secrets.bot_token }}" -PTG_CHAT_ID="${{ secrets.chat_id }}"

      - name: Upload debug arm64 apk
        uses: actions/upload-artifact@v4
        with:
          name: debug-arm64
          path: app/build/outputs/apk/debug/debug-arm64-v8a.apk

      - name: Upload debug armeabi apk
        uses: actions/upload-artifact@v4
        with:
          name: debug-armeabi
          path: app/build/outputs/apk/debug/debug-armeabi-v7a.apk
          
  send_debug_apks:
    name: Send Debug APKs
    runs-on: ubuntu-latest
    needs: build_debug_apk
    
    steps:
      - name: Download arm64 apk
        uses: actions/download-artifact@v4
        with:
          name: debug-arm64
          path: .

      - name: Download armeabi apk
        uses: actions/download-artifact@v4
        with:
          name: debug-armeabi
          path: .

      - name: Send arm64 APK to Telegram
        if: success()
        continue-on-error: true
        run: |
          curl -X POST "https://api.telegram.org/bot${{ secrets.bot_token }}/sendDocument" \
            -F chat_id="-1002951791106" \
            -F message_thread_id="4" \
            -F document=@"debug-arm64-v8a.apk" \
            -F caption="ARM64 build"

      - name: Send armeabi APK to Telegram
        if: success()
        continue-on-error: true
        run: |
          curl -X POST "https://api.telegram.org/bot${{ secrets.bot_token }}/sendDocument" \
            -F chat_id="-1002951791106" \
            -F message_thread_id="4" \
            -F document=@"debug-armeabi-v7a.apk" \
            -F caption="ARMEABI build"